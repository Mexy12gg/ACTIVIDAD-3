Juan Camilo Arevalo Gordillo 
/* Este codigo ha sido generado por el modulo psexport 32212967-w32 de PSeInt.
Es posible que el codigo generado no sea completamente correcto. Si encuentra
errores por favor reportelos en el foro (http://pseint.sourceforge.net). */

// En java, el nombre de un archivo fuente debe coincidir con el nombre de la clase que contiene,
// por lo que este archivo deber a llamarse "JUEGO_INTRODUCCION.java."

import java.io.*;
import java.math.*;

public class juego_introduccion {

	public static void main(String args[]) throws IOException {
		BufferedReader bufEntrada = new BufferedReader(new InputStreamReader(System.in));
		double acumulador;
		double contador;
		int moneda;
		int monto;
		String respuesta;
		String respuesta_2;
		String respuesta_3;
		String respuesta_4;
		double total_apuesta;
		double total_resta;
		double total_suma;
		contador = 0;
		acumulador = 0;